# Demo sandbox

- URL: `https://spoken-dev-brief.sociobot.in/demo` or local `http://127.0.0.1:4173/demo`.
- Sample: a sync retry discussion owned by Maya Chen, with two decisions, one assumption, one question, and two repository references.
- Storage namespace: every demo key begins `demo:spoken-dev-brief:`. Real work begins `real:spoken-dev-brief:`. Demo code never reads or writes real keys.
- Reset: choose **Reset demo** in the persistent banner. This clears only demo keys and restores the bundled sample.
- Exit: choose **Start for real**. Demo keys are discarded before the empty real workspace opens.
- Offline: visit the demo once, then reload it without a network connection.
